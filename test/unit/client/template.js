/**
 * Bundling Client Side Templates in main.js
 */
define([], function(){
	/**
	 * Should be same as template.js in assets/js/app/template.js
	 * Only difference will be the path of templates
	 */
});